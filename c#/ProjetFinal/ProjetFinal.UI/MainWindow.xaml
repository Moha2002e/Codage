<Window x:Class="Voyagera.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Voyagera.UI"
        mc:Ignorable="d"
        Title="Connexion - Voyegera" Height="450" Width="800"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Animation pour le bouton au survol -->
        <Storyboard x:Key="ButtonHoverStoryboard">
            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                            To="#FF0057" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="ButtonLeaveStoryboard">
            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                            To="#FF008C" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>

    <Grid Focusable="False">
        <!-- Fond en dégradé pour une allure moderne -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#00A8E8" Offset="0"/>
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Conteneur du formulaire de connexion avec animation d'apparition -->
        <Border Background="White" CornerRadius="10" Padding="20" Width="667"
                HorizontalAlignment="Center" VerticalAlignment="Center" Height="336" Margin="0,5,0,0"
                Focusable="False">
            <Border.RenderTransform>
                <TranslateTransform x:Name="loginTranslate" Y="30"/>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Border.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.5"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)"
                                             From="30" To="0" Duration="0:0:0.5"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <StackPanel>
                <TextBlock Text="Agence de Voyage Voyegera" FontSize="24" FontWeight="Bold"
                           Foreground="#007EA7" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="Nom d'utilisateur:" FontWeight="SemiBold" Margin="0,0,0,5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBox x:Name="txtUsername" Height="30" Margin="0,0,0,10"
                         BorderBrush="#007EA7" BorderThickness="1" Padding="5" Width="400" TextChanged="txtUsername_TextChanged" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" />
                <TextBlock Text="Mot de passe:" FontWeight="SemiBold" Margin="0,0,0,5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <PasswordBox x:Name="txtPassword" Height="30" Margin="0,0,0,20"
                             BorderBrush="#007EA7"  BorderThickness="1" Padding="5" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <Button Content="Se connecter" Height="40" Background="#FF00A7FF" Foreground="White"
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand" Width="400">
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.MouseEnter">
                            <BeginStoryboard Storyboard="{StaticResource ButtonHoverStoryboard}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Button.MouseLeave">
                            <BeginStoryboard Storyboard="{StaticResource ButtonLeaveStoryboard}"/>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>
                <TextBlock HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Hyperlink Click="OnHyperlinkClick">
                        <TextBlock Text="Vous avez un compte ?" Foreground="#007EA7" Cursor="Hand" />
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
